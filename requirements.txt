# Core dependencies (always required)
Flask>=3.0.0
Flask-CORS>=4.0.0
Flask-SQLAlchemy>=3.1.1
SQLAlchemy>=2.0.36
cryptography>=42.0.0
requests>=2.31.0
python-dotenv>=1.0.0
gunicorn>=21.2.0
psutil>=5.9.0
beautifulsoup4>=4.12.0

# Optional provider dependencies (install with: pip install ai-provider-service[providers])
# anthropic>=0.39.0     # For Claude provider
# openai>=1.54.0        # For OpenAI provider

# Testing dependencies
pytest>=7.0.0
pytest-flask>=1.2.0
pytest-cov>=4.0.0

# Optional for production deployment
# redis>=5.0.0          # For distributed model state (multi-instance setups)
# psycopg2-binary>=2.9.0  # For PostgreSQL instead of SQLite
